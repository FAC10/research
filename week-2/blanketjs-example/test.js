test('add(1,1) should equal 2', function(assert) {
  var result = add(1, 1); //no change/coins just an empty array
  var expected = 2;
  assert.deepEqual(result, expected);
}); // use deepEqual for arrays see: https://api.qunitjs.com/deepEqual/






















// Don't scroll further than this! Spoilers!
//
//
// test('multiply(1,1) should equal 1', function(assert) {
//   var result = multiply(1, 1); //no change/coins just an empty array
//   var expected = 1;
//   assert.deepEqual(result, expected);
// }); // use deepEqual for arrays see: https://api.qunitjs.com/deepEqual/
//
// test('subtract(1,1) should equal 0', function(assert) {
//   var result = subtract(1, 1); //no change/coins just an empty array
//   var expected = 0;
//   assert.deepEqual(result, expected);
// }); // use deepEqual for arrays see: https://api.qunitjs.com/deepEqual/
